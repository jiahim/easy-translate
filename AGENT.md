## Git 工作流

- 仅在用户明确要求提交、推送或创建 PR/MR 时执行对应 Git 操作。
- 每次开始开发前，先确认本次工作的公共开发分支；从该分支获取最新代码后，再基于最新状态创建或更新专属私有分支。公共开发分支以用户指定或仓库当前约定为准，例如 `release_v0.1.0_20260824`。
- 日常开发和本地提交只能发生在专属私有分支，严禁直接提交到公共开发分支。私有分支建议命名为 `<公共开发分支>-<姓名或工号>`；Codex 自建分支仍遵循环境要求的 `codex/` 前缀。
- Codex 创建的所有 commit message 必须以 `[AI] ` 开头，例如 `[AI] 自动补全接口文档`。提醒用户：人工创建的 commit message 应以 `[Human] ` 开头，例如 `[Human] 修复登录页样式问题`。
- 同步公共开发分支更新时优先使用 `rebase`，需要保留合并语义时可使用 `merge`。操作前先检查工作区状态，不覆盖或丢弃现有未提交改动。
- 功能完成后，只能通过“私有分支 → 公共开发分支”的 PR/MR 合入；禁止直接推送代码到公共开发分支。
- 创建 PR/MR 前必须解决全部冲突，并运行与改动范围相匹配的检查或测试，确认合并后代码可正常运行。只有代码评审通过且冲突完全解决后方可合并。
- 每天开始工作时，先获取公共开发分支最新更新并同步到私有分支，以减少后续冲突。
- 保持提交粒度合理、主题单一，避免在一个提交中混入无关变更。